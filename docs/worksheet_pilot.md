# تجربة ورقة المعادلات عبر واتساب — مغلقة افتراضيًا

تعتمد هذه المسودة على PR #1، رأس `a2399c7c5ecf3f328cb06d5182fdfb12d5fd8cf5`.
قاعدتها فرع `codex/document-templates-pilot`؛ لا تُدمج في فرع القوالب تلقائيًا.
بعد قرار تركي بدمج القوالب، يعاد توجيه قاعدة هذه المسودة إلى main، وفحص فرقها ورأسها مجددًا.

## النطاق ومسار الطلب

المحتوى هو review-v2 الأصلي المعتمد في محادثة مساعد المعلم: ثمانية بنود للسادس،
حساب ذهني وتخمين وتحقق، بلا درجات. الأسئلة والرسم والجدول والمعادلات والإجابة
مطابقة لملفات التجربة المحلية؛ أزيل اسم المعلم من ملف المحتوى العام فقط، ويُحقن
الاسم المؤكد «تركي عايد الحارثي» عند التخصيص. بقية البيانات غير المؤكدة فارغة.
لا تنزيل من تحضيري، ولا نسخ أسئلة/شعارات مصادر، ولا ادعاء مطابقة طبعة المنهج.

بعد التفعيل المصرح به فقط، يرسل الرقم الموثق أمر `ورقة المعادلات`.
يعترضه مسار `/webhook/whatsapp` قبل GPT، يتحقق من توقيع Meta ورقم شواهد
ورقم المستلم، ويحجز معرف الرسالة في SQLite ثم يولد PDF الطالب والإجابة
في خيط منفصل. يجب نجاح الملفين قبل أي رفع. يرفعهما إلى Meta كوسيط خاص
ويرسلهما منفصلين للمعلم، دون رابط عام. مراجعة المحتوى لهذه التجربة هي اعتماد
review-v2؛ تعديل المدرسة صريح من المعلم عبر:

`مدرسة ورقة المعادلات: اسم المدرسة`

يُعيد هذا الأمر التوليد ويحفظ نسخة جديدة. لا يغير الأسئلة أو ملف حساب المعلم
العام؛ بيانات التخصيص محلية للتجربة ومفصولة بمفتاح مالك مشتق من رقم المرسل.
هذه أوامر محددة، وليست فهمًا عامًا لأي صياغة لطلب أوراق عمل.

## الخصوصية والحالات والفشل

- `created`: الملف كُتب، مع SHA-256. لا يعني الإرسال.
- `accepted`: Meta أعادت معرف رسالة. لا يعني أنها وصلت.
- `sent`: إشعار موقع من Meta بالإرسال.
- `delivered`: إشعار وصول أو قراءة موقع؛ لا يعني استخدامًا صفيًا.
- `export_failed`: لم يبدأ أي رفع/إرسال، حتى عند فشل ملف الإجابة بعد نجاح الطالب.
- `uncertain`: خطأ في الرفع/الإرسال؛ توقف تحفظي، ولا إعادة محاولة آلية.

SQLite يحجز الرسالة ذريًا قبل إنشاء المهمة، ويمنع التكرار بعد إعادة التشغيل.
عامل واحد فقط يستحوذ على المهمة. عند انقطاع العملية تبقى queued/working أو
uncertain للمراجعة اليدوية؛ لا يعد هذا نظام طابور ذي استعادة تلقائية. بعد بدء
طلب الإرسال لا يمكن ضمان exactly-once عبر الشبكة: نفضل توقف التسليم على خطر
التكرار. يُمنع بدء طلب آخر للمالك ما دامت مهمة غير محسومة. الرسائل المحجوزة
أثناء الانشغال تُسجل blocked ولا يعاد تنفيذها خفية. عند الفشل يفحص المشغل
السجل المحلي؛ لا توجد رسالة واتساب آلية تدعي نجاحًا أو تستخدم مسار stub.
إشعار الوصول السابق لرد الإرسال يُحفظ ويصالح لاحقًا؛ الإشعارات لا تخفض delivered.

لا توجد واجهة تنزيل عامة أو API تعرض السجل. يرفض الكود التخزين داخل STORAGE_DIR
المتاح عبر `/files` أو `app/static`. يلزم **تخزين خاص دائم مستقل** قبل التفعيل؛
لا تستخدم `/app/storage` العام الحالي. لا توضع أسرار أو أرقام حقيقية أو مخرجات
المعلم داخل Git. لا توجد سجلات استخدام صفي أو أثر تعليمي في هذا المسار.

## الفحوص المحلية وإعادة الإنتاج

على رأس PR #1 المذكور: 16 اختبار قوالب وتصدير فعلي و32 اختبار محرك نجحت.
على تنفيذ الربط محليًا: نجحت 67 تجربة (19 للربط + 16 للقوالب + 32 للمحرك)
في 34.50 ثانية، مع خمسة تحذيرات deprecation من PyMuPDF دون فشل.
شُغّل PR #1 أيضًا بواسطة Uvicorn على 127.0.0.1:18010، واستُدعي health عبر
HTTP وأعاد 200 healthy، ثم أُوقف. هذا فحص Windows محلي وليس حاوية Linux.
المحاكي أنتج أربع صفحات PDF فُحصت بصريًا؛ صورتا النسخة الأولى تطابقان تصيير
PDF المعتمد review-v2 بكسلًا ببكسل. النسخة الثانية لا تختلف في البيانات إلا
بمدرسة تجريبية، مع بقاء بنود الأسئلة الثمانية والمحتوى دون تغيير.
`/health` أعاد 200 healthy عبر تطبيق FastAPI محلي مع SQLite مؤقت وأسرار فارغة.
Docker Desktop مثبت لكن Engine غير متاح؛ محاولة build فشلت قبل البناء لغياب
named pipe. **هذا ليس نجاح فحص حاوية**. أضيف workflow يبني الصورة ويشغلها
بشبكة معطلة ويختبر health والقوالب والربط والمحرك، ثم يكرر فحص PR #1 على
الرأس المحدد. لا يحتوي workflow على نشر أو أسرار إنتاج. يلزم قراءة نتيجته الفعلية.

اختبارات `tests/worksheet_pilot` تشمل المسار الحقيقي HTTP→PDF مع محاكاة Meta،
التخصيص وإعادة التوليد، ملكية البيانات والإشعارات، التوقيع والمفتاح المغلق،
عدم تسرب الإجابات، الفشل، تكرار الطلب بعد إعادة فتح السجل، التزامن، وسباق
إشعار الوصول مع رد الإرسال. ليست تجربة شبكة Meta فعلية.

```sh
SHAWAHID_PDF_TESTS=1 python -m unittest discover -s tests/worksheet_pilot -v
SHAWAHID_PDF_TESTS=1 python -m unittest discover -s tests/document_templates -v
python -m pytest --noconftest tests/test_exam_engine.py -q
python scripts/simulate_worksheet_pilot.py --output /absolute/private/outside/repo/trial
```

المحاكي لا ينشئ عميل Meta؛ يولد ملفين ثم ملفين بعد تغيير مدرسة وهمية، مع صور
وJSON وسجل `OFFLINE_SIMULATION`. بيانات المدرسة في مثال التعديل ليست حقيقة.

## النشر والتفعيل المحدود — لا ينفذها هذا PR

فحص Railway للقراءة فقط بتاريخ 2026-09-22 أظهر مشروع shawahid-service،
خدمة shawahid-service، بيئة production، مصدر GitHub هذا المستودع وفرع main،
و`checkSuites=false`، ونسخة تشغيل واحدة، ومجلد volume عام `/app/storage`.
الإعداد المحفوظ يعرض RAILPACK، بينما railway.toml يطلب DOCKERFILE؛ يجب مراجعة
إعداد البناء الفعلي قبل قرار النشر. ارتباط main يعني التعامل مع الدمج كعملية
يمكن أن تطلق نشرًا، ولا يُفترض أن فحوص GitHub ستؤخره. لم تتغير الإعدادات.
start.sh يشغّل alembic upgrade head إذا ضُبط DATABASE_URL، رغم عدم إضافة
أي ترحيل قاعدة بيانات في هذه المسودة. لا تجعل مراجعة هذه المسودة موافقة نشر.

بعد نجاح الفحوص ومراجعة تركي وقرار منفصل منه:

1. ثبّت الرؤوس المعتمدة، واحسم auto-deploy وخطة النسخ الاحتياطي في شواهد فقط.
2. جهز تخزينًا خاصًا دائمًا صالحًا لـSQLite بنسخة خدمة واحدة، خارج جميع مسارات
   الملفات العامة، واضبط WORKSHEET_PILOT_PRIVATE_DIR. لا تفقد سجل منع التكرار عند النشر.
3. تحقق من رقم تركي من قناة موثوقة ومن هوية رقم Meta التجاري لشواهد؛ لا تستنتجه
   من SUPPORT_PHONE أو README. احفظ رقم المستلم في WORKSHEET_PILOT_PHONE،
   ومرجع التحقق وتاريخه في WORKSHEET_PILOT_VERIFICATION_REF وWORKSHEET_PILOT_VERIFIED_AT.
   قيمها الافتراضية فارغة ولم يُثبت الرقم في هذه التجربة.
4. اضبط WHATSAPP_APP_SECRET بالمسار الآمن للأسرار، وتحقق من إصدار Meta المدعوم
   للحساب وإشعارات الحالة. لا تنسخ الأسرار إلى الدردشة أو Git. لم يتغير الإصدار الحالي.
5. انشر أولًا مع WORKSHEET_PILOT_ENABLED=false وتحقق health وهوية شواهد والرأس.
6. بعد إذن التفعيل اضبطه true، ثم فقط اطلب رسالة الاختبار من تركي، وافحص
   الملفين والحالات وإعادة التخصيص. لا ترسل إلى أي رقم آخر.

التراجع: أغلق المفتاح وأعد تشغيل الخدمة لإيقاف قبول طلبات التجربة. عند إرسال
جارٍ أوقف العامل قبل التراجع إذا لزم الإيقاف الفوري؛ تغيير متغير البيئة وحده
لا يلغي اتصالًا بدأ بالفعل. احتفظ بالسجل والملفات الخاصة للتحقق ومنع التكرار،
ثم عد إلى الإصدار السابق المعتمد بقرار النشر. لا تمس مشروع نحلة أو قاعدة بياناته.
الوصول والاستخدام الفعلي يظلان غير موثقين حتى يرد الدليل من تركي.

مرجع حالات Meta:
https://www.postman.com/meta/whatsapp-business-platform/folder/tduohwq/webhook-payload-reference
ومرجع التوقيع:
https://whatsapp.github.io/WhatsApp-Nodejs-SDK/api-reference/webhooks/start/
