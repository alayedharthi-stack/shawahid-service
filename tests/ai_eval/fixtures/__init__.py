"""
Real fixture data for the AI Evaluation Suite.

Layout:
    fixtures/
        text/messages.json         ← real Arabic samples (source of truth)
        images/                    ← real JPEGs generated by ``fixture_builder``
        audio/                     ← real WAVs generated by ``fixture_builder``
        pdfs/                      ← real (small) PDFs generated by
                                     ``fixture_builder`` when reportlab/fitz are
                                     available; otherwise the eval reads the
                                     ``extracted_text`` field from the dataset
                                     directly.

Run the fixture builder once before evaluating::

    python -m tests.ai_eval.fixtures.fixture_builder
"""

from pathlib import Path

FIXTURES_ROOT = Path(__file__).resolve().parent
PDFS_DIR = FIXTURES_ROOT / "pdfs"
IMAGES_DIR = FIXTURES_ROOT / "images"
AUDIO_DIR = FIXTURES_ROOT / "audio"
TEXT_DIR = FIXTURES_ROOT / "text"

for d in (PDFS_DIR, IMAGES_DIR, AUDIO_DIR, TEXT_DIR):
    d.mkdir(parents=True, exist_ok=True)
